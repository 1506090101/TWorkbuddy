# Agent Buddy 开发规范

## 产品目标

Agent Buddy 面向需要长期维护代码库的开发者，提供以任务为单位的 Agent Workbuddy：项目、会话目标、Agent、模型、附件、插件和工具在一个可恢复工作台内协作。对话只承载指令和决策；文件变更、工具执行、权限、测试和结果必须作为可审查工作事件呈现。产品优先保证可控性：敏感信息不离开主进程，用户显式选择上下文，高风险操作可确认/拒绝，所有长流程都有状态、取消和失败反馈。完整方向以 `agent-workbuddy-v2.md` 为准。

## 需求先行

当前阶段先完成需求文档同步和评审，再进入实现。`agent-buddy-prd.md` 负责产品定位与 MVP，`agent-buddy-design.md` 负责布局和数据流，`features/` 负责可执行验收，`AGENTS.md` 负责工程约束。旧聊天方案保留为历史背景，不作为新功能的默认交互。

V2 主界面固定为：左侧工作导航、顶部项目/会话/Agent/模型/目标上下文、中央 Agent 工作流时间线、底部 Agent Composer、右侧目标/工具/变更/结果面板。Codex Desktop 仅提供信息架构参考，不复制品牌或像素级视觉。

## 交付路线

开发顺序以 `features/README.md` 的 V2 首轮交付顺序为准，不跨越未完成的安全或数据边界。先交付持久化 `WorkSession` 和 `WorkEvent`，再交付 Agent 工作流时间线、Agent Composer、工具权限、会话模型覆盖、Agent 切换和会话目标；然后继续项目感知、Git、变更审查、复杂编排和生态能力。Phase 编号保留完整路线图，但不允许因旧“聊天 UI”顺序绕开工作台闭环。

当前基线：V2 首轮已具备主进程 `WorkSession`/`WorkEvent` 持久化、工作流时间线、Composer、会话模型覆盖、项目目录关联、七个内置工具及分级权限；也已提供四个预设 Agent 的会话级切换、可编辑的会话目标/步骤、缓存的项目技术栈摘要、只读 Git 状态上下文，以及 Checkpoint/diff 审查。F1.12 将同一任务的 Agent 写入汇总为持久化变更集；F1.13 已在工作流时间线中呈现内联 diff 与任务级审查；F1.14 已在 Provider 调用前落实五类上下文预算、自动/手动历史压缩和右侧用量面板；F1.15 已持久化每次 Provider 用量、会话 token/成本摘要、月度预算预警，并提供设置页模型定价编辑；F1.16 已提供命令面板、任务快捷键和 Composer 斜杠上下文命令；F1.17 已提供主题、字体 token 和受限全局快捷键设置；F2.2 已提供手动分支、受限上下文摘要、树形关系、合并/丢弃和父任务时间线结果；F2.4 已补齐目标停止、重新规划、阻塞原因与关联事件筛选。下一步继续复杂多 hunk、rename 与大 diff 性能，并推进 F2.3 Agent 结果展示。Feature 文档中的 `Status`、V1 历史事实和 V2 验收 checkbox 必须与代码同步维护。

## 功能开发闭环

每项 Feature 按以下顺序实现：

1. 先阅读 `agent-workbuddy-v2.md`、关联 Feature、依赖项和现有类型，确认工作会话、上下文、事件和 IPC 边界。
2. 在 `shared/` 定义跨进程类型，在 `main/` 实现持久化、校验与运行时，在 `preload/` 暴露最小 API。
3. 在 `renderer/src/stores/` 管理会话、Composer 与审查状态；工作台组件实现加载、空数据、错误、成功、取消和禁用状态。
4. 按验收标准验证“新建任务 → 组织上下文 → Agent 工作 → 审查/确认 → 恢复会话”闭环，再更新 Feature 文档和本文件状态。

### 文档验收规范

- 每项需求必须写清目标用户、前置依赖、数据边界、主进程事实来源、失败/取消路径和桌面端验收步骤。
- `WorkSession`、`ComposerContext`、`Goal`、`WorkEvent` 的命名和字段含义在 PRD、设计和 Feature 文档中保持一致。
- Feature 完成前不得勾选 V2 验收项；勾选必须同时具备类型/IPC/UI 证据和人工工作流验证。
- 需求调整需要新增“V2 需求调整”与迁移说明，不删除旧需求，不直接修改实现作为替代。

### 分支任务垂直切片

F2.2 首轮实现必须从主进程创建 `WorkSession.branch`，只复制项目、Agent、模型覆盖、插件 id 和重新编号的目标；不得复制附件原文、完整事件历史或权限决定。分支上下文只通过受限摘要注入 Agent，摘要编辑、合并、丢弃和归档都必须走 preload → IPC → 主进程持久化。合并只能幂等地产生一条父任务 `summary` 事件；丢弃只影响子任务。新增分支能力后必须验证“创建 → 重启恢复 → 独立打开 → 合并/丢弃”的桌面端闭环。

## 工程质量标准

- API Key 只能在主进程解密和使用；renderer 只接收脱敏配置。
- `WorkSession`、目标、模型覆盖、权限决定和工作事件由主进程持久化；renderer 不得成为唯一事实来源。
- 主进程 IPC handler 必须校验 Composer 附件、Agent、模型、插件和目标引用，返回明确错误，并设置网络超时。
- Agent 工具范围是会话运行时的白名单；即使 Agent 允许某工具，写入和命令执行仍必须经过 F1.6 权限确认。
- 目标状态、步骤和结果的人工变更必须经主进程持久化，并写入可审查 `WorkEvent`；不可只更新 renderer store。
- 目标停止、重新规划和阻塞原因同样必须由主进程持久化；新工作事件应携带当前 `goalId`，时间线筛选只能消费该关联，而不是由 renderer 按文本猜测。
- 项目检测仅由主进程读取有限配置和常见路径；缓存存放在用户配置目录，注入模型前仅保留脱敏、限长的项目摘要。
- Git 基础上下文只能使用无 shell 的只读子进程；任何提交、分支、暂存、回滚或 diff 应用必须在 F1.11 的审查与权限边界上实现。
- 文件写入或编辑在权限确认后、落盘前必须创建项目内相对路径的 Checkpoint；审查、拒绝和回滚都只能在主进程恢复该快照，renderer 不得直接写文件。
- 多文件审查必须从会话内的 `Changeset` 聚合生成；文件原因应来自最近用户任务，文件级/整批决定必须同步更新 hunk 状态和 `WorkEvent`，整批恢复后不得遗留无关联的 Checkpoint。
- 时间线内联 diff 必须只消费 `file_change` 的 `changesetId`，并复用主进程审批 API；hunk 定位必须同时携带 Changeset、相对路径和 hunk 索引。审批快捷键只能在变更卡片焦点域拦截，禁止劫持 Composer 的 `Ctrl+Z`。
- Provider 请求的系统提示和历史必须经主进程上下文预算构建；预算配置需校验来源分配，自动或手动压缩只能改变运行时 Agent 历史并记录 `WorkEvent`。renderer 只能读取 `ContextUsage` 并请求压缩，不得读取或重写完整历史。
- Provider 调用成功后由主进程记录 `ModelUsageRecord`；真实 usage 优先于估算，缺失 usage 必须标记估算。模型价格只允许显式按 Provider/模型配置，未定价记录必须显示为 token 计量而非伪造美元成本；预算预警须落为 `WorkEvent`。
- 全局快捷键只注册工作台级动作，并以 `Ctrl`/`Cmd` 兼容；不得拦截 Composer 的 Enter、Shift+Enter、复制、粘贴、撤销，也不得扩大既有变更审查快捷键的焦点域。斜杠命令只复用会话、Agent、模型、项目、目标和附件入口，不能绕过工具权限或直接执行命令。
- 主题、字号、代码字体和工作台快捷键覆盖属于 renderer UI 偏好，必须通过 `uiStore` 持久化并同步应用 CSS token/快捷键注册表。快捷键录制应要求 Ctrl/Cmd 或 Alt，检测冲突并拒绝复制、粘贴、剪切、全选、撤销和重做；不得在设置页扩大系统级、工具或变更审批的快捷键范围。
- Provider 调用成功后由主进程记录 `ModelUsageRecord`；真实 usage 优先于估算，缺失 usage 必须标记估算。模型价格只允许显式按 Provider/模型配置，未定价记录必须显示为 token 计量而非伪造美元成本；预算预警须落为 `WorkEvent`。
- UI 统一使用现有设计 token、公共组件和 `lucide-react` 图标；工作台优先紧凑、可扫描和可操作，避免营销式大卡片和聊天产品语义。
- 共享类型不得导入 Electron、Node 或浏览器专属模块；插件选择不代表工具授权。
- 改动完成后在 `agent-buddy/` 运行 `npm run typecheck`、`npm run lint`、`npm run build`。
