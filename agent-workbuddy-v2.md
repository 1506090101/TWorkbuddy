# Agent Buddy V2 - Agent Workbuddy 需求调整

> **状态**：需求基线，待实现
> **日期**：2026-08-02
> **设计参考**：参考 Codex Desktop 的信息架构与工作流交互，不复制其品牌、文案或像素级界面。

## 1. 产品定位

Agent Buddy V2 是面向开发任务的本地 Agent 工作台，不是通用 LLM 聊天工具。用户从一个项目或任务开始，配置 Agent、模型、文件、插件和目标；Agent 在同一工作区内读取上下文、执行工具、产出文件变更和验证结果，用户能在每个关键节点查看、确认、停止或调整工作。

对话流仍然存在，但它是任务指令、决策记录和工作结果的时间线，不是产品的唯一界面或价值中心。

### V2 成功标准

用户能够完成以下闭环：新建任务 -> 选择项目、Agent 和模型 -> 附加图片或文本/代码文件 -> 设置会话目标 -> 启用可用插件上下文 -> 发起工作 -> 观察工具、变更和验证事件 -> 在高风险操作处确认或中止 -> 重新打开会话继续工作。

## 2. 工作台信息架构

```
┌──────────────────────────────────────────────────────────────┐
│ 应用栏：项目 / 当前任务 / Agent / 模型 / 全局操作             │
├───────────────┬──────────────────────────────┬────────────────┤
│ 工作导航      │ Agent 工作流时间线            │ 上下文面板     │
│ - 新建任务    │ - 用户任务与决策              │ - 会话目标     │
│ - 会话列表    │ - Agent 思考与进度            │ - 任务步骤     │
│ - 项目入口    │ - 工具调用与权限确认          │ - 工具活动     │
│ - Agent/插件  │ - 文件变更、测试、结果总结    │ - 文件变更     │
├───────────────┴──────────────────────────────┴────────────────┤
│ Agent Composer：+ 文件 | 模型 | 插件 | 目标 | Thinking | 发送 │
└──────────────────────────────────────────────────────────────┘
```

- **左侧工作导航**：新建任务、最近会话、项目入口、Agent 与插件入口。会话显示标题、最近活动、运行状态和目标状态，而不是聊天联系人。
- **顶部上下文栏**：显示当前项目、任务标题、活动 Agent、会话生效模型与目标摘要；所有信息均可打开对应编辑入口。
- **中央工作流时间线**：按事件展示用户指令、Agent 输出、思考、工具调用、权限决策、文件变更、测试结果、错误与最终总结。普通消息不使用营销式大气泡；工具和变更使用紧凑可展开的操作记录。
- **右侧上下文面板**：可折叠，默认显示目标与步骤；可切换查看工具活动、文件变更和 Agent 结果。
- **Agent Composer**：工作台的统一操作入口，始终包含模型、文件、插件、目标与发送控制。

## 3. 会话与 Composer 约定

### 3.1 WorkSession

`WorkSession` 是主进程持久化的工作单元，绑定项目上下文，可被恢复和继续。它包含：

- 标识与元数据：`id`、标题、项目引用、创建/更新时间、运行状态；
- 工作上下文：活动 Agent、默认模型分配的会话覆盖、会话目标；
- 工作记录：时间线事件、附件元数据、工具调用、权限决策、文件变更与验证结果；
- 未来能力：分支、摘要、导出与跨会话搜索。

重启后恢复最近会话和其工作记录；密钥、文件正文和系统能力不暴露给渲染进程。

### 3.2 ComposerContext

每次发起工作使用显式的 `ComposerContext`：

- `agentId`：当前会话 Agent；
- `modelOverride`：当前会话即时模型选择，仅覆盖本次会话，不改 Agent 默认分配；
- `thinkingLevel`：当前会话推理等级；
- `attachments`：图片、文本与代码文件；图片进入 Vision 路由，文本/代码以受限上下文块进入 Agent；
- `pluginIds`：已启用的插件上下文；首轮只记录选择与可用状态，真实执行遵循工具/插件 Feature；
- `goalId`：关联的会话目标。

### 3.3 文件与安全边界

首轮允许图片、UTF-8 文本和代码文件。文件选择器必须展示名称、类型、大小和移除操作；图片与文本/代码分别校验类型和大小。二进制任意附件、静默读取本地文件、未经确认的写入或命令执行均不属于 Composer 首轮范围。

### 3.4 目标

`Goal` 按会话保存，包含标题、描述、状态（`draft`、`active`、`blocked`、`completed`）、可选步骤和完成结果。Composer 可创建或关联目标，顶部栏和右侧面板持续展示当前目标。复杂拆解、并行编排和子 Agent 委派仍由 F2.4 实现。

## 4. V2 概念模型

| 概念 | 责任 |
|------|------|
| `WorkSession` | 持久化任务、项目和工作记录的边界 |
| `WorkEvent` | 统一时间线事件：消息、思考、工具、权限、变更、测试、错误、总结 |
| `ComposerContext` | 一次 Agent 请求携带的可见上下文与会话覆盖 |
| `Goal` | 当前会话的可见目标与后续任务计划锚点 |
| `SessionModelOverride` | 当前会话即时模型与 Thinking 设置，不改全局/Agent 默认值 |
| `PluginContext` | 用户显式启用的插件引用与可用状态，不等同于已授予工具权限 |

## 5. Feature 重排

| 优先级 | Feature | V2 责任 |
|------|---------|---------|
| P0 | F1.3 | 可恢复的 Agent Session 与统一 `WorkEvent` 事件流 |
| P0 | F1.4 | Agent 工作流时间线、操作记录、结果与审查展示 |
| P0 | F1.5 | Agent Composer、文件上下文、模型/插件/目标入口 |
| P0 | F1.6 | 工具执行、权限确认、工具活动与失败恢复 |
| P0 | F1.8 | 默认模型分配与会话即时覆盖 |
| P1 | F2.1 | Agent 管理与工作台中的 Agent 切换 |
| P1 | F2.4 | 会话目标、步骤、进度和后续编排 |

F1.2 继续负责图片路由；F1.7 继续负责 Provider 管理。F1.9、F1.10、F1.11-F1.13 依次为工作台补齐项目感知、Git、变更审查和应用闭环。

## 6. V2 验收

- 用户可新建、保存、恢复和切换工作会话。
- Composer 可选择当前会话模型、Thinking、文件、插件上下文和会话目标。
- 中央时间线清晰区分指令、Agent 输出、工具活动、权限、变更、测试与错误。
- 高风险工具操作需要用户确认；停止操作能中止当前运行。
- 目标、工具和文件变更可在右侧面板审查，且不打断主工作流。
- 浅色紧凑工作台为默认视觉基调，同时保留深色主题和现有安全边界。

## 7. 迁移规则

- 原“聊天 UI”“InputBar”等术语在 V2 中分别迁移为“Agent 工作流时间线”“Agent Composer”。旧术语仅作为代码兼容名称或历史记录保留。
- 既有 Provider、Vision 路由和 Agent Session 实现视为 V2 底座，不因需求调整标记为 V2 完成。
- 已完成的验收项保持历史事实；所有新增 V2 验收项默认未完成，直到代码、测试和人工工作流验证共同证明。
- 本文档是产品方向与跨 Feature 约定的权威来源；具体接口、实现和验收以关联 Feature 的 V2 章节为准。
